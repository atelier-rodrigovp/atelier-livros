[build]
  command = "npm run build"
  publish = "dist"

# SPA: todas as rotas caem no index.html (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
