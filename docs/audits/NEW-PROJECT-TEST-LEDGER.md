# Ledger de testes — Novo Projeto

## Baseline 2026-07-11

- Hipótese: a suíte atual está verde, mas não cobre integralmente o percurso iniciado em
  `NovoProjeto.tsx`.
- Evidência: 38 arquivos e 351 testes aprovados; nenhum teste encontrado ainda para o
  contrato completo da entrevista/fundação.
- Lint: 0 erros e 3 warnings conhecidos.
- Build e typecheck: aprovados.
- Python/paridade: aprovados no escopo atual.
- Próximo ciclo: inventariar cobertura e reproduzir as lacunas P1/P2 sem tocar no livro vivo.

| Ciclo | Hipótese | Teste | Antes | Correção | Depois | Risco residual |
|---|---|---|---|---|---|---|
| 0 | baseline hermético | suíte completa | 351 aprovados | nenhuma | 351 aprovados | percurso Novo Projeto incompleto |
| 11 (2026-07-13) | rodada final H1–H7 + SKILL_DRIFT | testes determinísticos (gate fake-Claude, breaker, faixa) + 1 execução real com Claude em fixture | H1 confirmada (1 tentativa, ignorava max_iteracoes); H2 confirmada (orquestrador editava prosa); H3 confirmada (bloqueava antes do micro-loop); H4 confirmada (aceitação não recontava moldes/muletas); H5 parcial (no-op silencioso com spec incompleta); H6 confirmada (sem breaker de erro idêntico); H7 confirmada (piso 2800 = teto da skill hoover, origem: arquiteto) | gate = loop convergente delegado ao livro-escritor com pendências p/ micro-loop; guarda reconta tudo e bloqueia cirúrgico; estado-editorial avisa alto; registrarErroRepetido (3x ⇒ para legível); FAIXA_PALAVRAS_POR_SKILL no fundacao-gate + cae 2800→2000 (ajuste-piso.json); gerar-manifest.ts + teste anti-drift (pegou esquecimento real na 1ª oportunidade) | outcome real: excesso 5→1→0 em 2 iterações (recontagem pegou molde REINTRODUZIDO na iter-1 e a iter-2 o eliminou); aprovado sem pendências; cleanup 0/0/0; worker sobe limpo manifest 1.0.2; suítes 387+439 | 1ª tentativa da fixture falhou por permissão de escrita no path temporário (acceptEdits não cobre 8.3 paths) — 2 chamadas Claude perdidas, documentadas; H5: campos de spec seguem dependentes do editor preencher |
| 10 (2026-07-13) | validar 53abdade (família cadência) antes da despausa | autópsia cap-37 + 5 fixtures de paridade + gate re-executado + ensaio de retomada | 7 ocorrências classificadas: 4 FP (anáfora por artigo "O" com substantivos ≠; 3 frases completas de 3 palavras contadas como fragmento), 3 reais (Travou./Comprometido./Ele-Ele dentro do alvo) | anáfora: chave de 2 palavras quando a 1ª é funcional; fragmento: 1-2 palavras ou 3 com negação (TS+Python, paridade 14/14) | cap-37 real APROVADO (hash `a2f25eb3` inalterado); reset stale PROVADO (blocked→pending em run de tmp, sem Claude); retomada determinística: cae revisa cap-02→escreve cap-03, 53ab revisa cap-37→escreve cap-38 (piso 1800; cap-37 válido); worker 378 + raiz 430 verdes; runner instalado `f7712e66` | jobs paused não são reclaimados — religar exige novo escrever_livro (botão Continuar escrita); calibração de beat de 3 palavras sem negação deixa de contar (decisão documentada) |
| 9 (2026-07-13) | loop de correção não convergia (cae6a074 cap-02): gate detecta mas nunca aprova | autópsia do caso real + 6 fixtures de paridade + gate re-executado no capítulo real | iter0: fragmento antitetico 8x + 5 famílias acima (instrução genérica, 1616 chars, sem linhas); iter1: agente zerou 6/7 famílias, 8→3 na bloqueante, e MANTEVE de propósito antítese de diálogo legítima; classificação manual: 4/5 marcações = falso positivo (regex sem exigir antítese); recontagem sobre texto certo (hash `7b24acd6` confere) | regex exige 2º termo antitético (TS+Python espelhados); prompt cirúrgico com (linha, trecho) exatos; blockers nomeiam frases; flag `blocked_quality` stale limpo no início do run | iter2 (mesmo texto): 1 real ≤ budget → GATE APROVADO, ledger atualizado; paridade 9/9; raiz 425 + worker 373 verdes; runner instalado = repo (`bb5feccb…`) | 53abdade reprova por CADÊNCIA (família não classificada caso a caso); vazamento REFUTADO (A24: string só em quality_reason/UI, acervo limpo ✓) |
| 8 (2026-07-13) | DDL do reliability.sql sem via de aplicação | aplicação via SQL Editor (sessão de browser autorizada, CDP) + 2 testes negativos | ANTES: dedupe ACEITO; sem trigger/índice/guard (snapshot em pre-sql-snapshot.sql) | arquivo inteiro aplicado em ordem (HTTP 201) | DEPOIS: índice+trigger+guard presentes; `set_config` antes do update; UPDATE lateral → `P0001 … promocao transacional` (rollback, 0 restam); 2º insert → `REJEITADO … jobs_one_queued_per_project_tipo_uidx`; probes limpos 0/0 | quota da org Supabase excedida (graça até 2026-08-07) — operacional, monitorar |
| 7 (2026-07-12/13) | E2E real da FUNDAÇÃO: 1ª execução BLOQUEADA pelo gate novo (`AGENTE_AUSENTE:*` — arquiteto gravou agentes em staging `_agentes-para-instalar/` porque a sessão headless bloqueia escrita em `.claude/`); retry com autocura APROVADO | execução real via harness + `fundacao-refino.test.ts` (staging, +2) + `fundacao-gate.test.ts` (parser, +1) | antes do gate isso passava em silêncio (projeto nascia sem agentes); parser não lia formato real `### Cap. N` | `instalarAgentesDeStaging` (determinístico, nunca sobrescreve) na frente dos normalizadores; regex calibrada com artefato real | retry: 5 agentes instalados, modelos pinados, craft injetada, GATE `approved` 0 blockers, Estrutura 14==ESTADO 14, hash `9aca6fb8…`; projeto `fundacao`; Storage com 7 arquivos (incl. quality state) | teto de retry continua o do job (max_attempts); staging só reconhece `livro-*.md` |
| 6 (2026-07-12) | E2E controlado: criação na UI real → entrevista com validador novo → conclusão → fundação enfileirada e retida pela pausa | projeto efêmero `AUDIT-CODEX-NEW-PROJECT-20260712` + harness `audit-run-job.ts` (claim RPC; produção global intocada) | UI antiga: spinner sem explicação com worker pausado (F-04 vivo) | 5 turnos reais de Claude; 12 respostas = teto exato; briefing validado (cobertura+coerência); `criar_fundacao` auto-enfileirada e bloqueada pela fila | banco+disco+UI conferidos; dry-run de limpeza = 1 objeto | fundação real + 1 capítulo pendentes de decisão do autor (produção pausada; custo Opus/Max); limpeza adiada até lá |
| 5 (2026-07-12) | fila podia matar job de fome (prioridade sem aging) e duplicar enqueue em corrida; UI mostrava execução fantasma | `fila.test.ts` (+3), `status.test.ts` (+2), `reliability-sql.test.ts` (+2) | anomalia real: 2 jobs queued 14 dias (causa confirmada: producao_pausada — não bug); starvation e dupla-inserção demonstradas por análise de código | `prioridadeEfetiva` (+1 prioridade/24h de espera); índice único parcial de queued por projeto+tipo; `displayProjectStatus.qualityBlocked`; cerca anti-injection no prompt da entrevista (P2 de segurança) | worker 358 + total 409 testes verdes; build ok | DDL do índice e do trigger de `pronto` pendentes de aplicação manual pelo autor; segurança: P3s registrados (buckets public=false a confirmar, audit-backup no repo, segmentos humanos fora da guarda de path) |
| 4 (2026-07-12) | projetos antigos sem Quality State ficariam impublicáveis sem caminho de migração; redução de qualidade era 1 clique silencioso | `worker/src/backfill-quality.test.ts` (4 testes) | sem quality/capitulo-NN.json ⇒ `QUALITY_STATE_MISSING` (bloqueio correto mas sem saída) | backfill honesto (mede muleta/cadência; `pending`/`rewrite_required`, nunca `approved`); confirm+timestamp na UI p/ desligar micro-revisão; rótulo `reducao_qualidade` no job; comentários "teto aceita" corrigidos no runner | typecheck ok; worker 352 testes verdes | backfill não cobre spec/continuidade/agência (explícito no estado); runner versionado ≠ instalado até rodar `instalar-skills.ps1` (só comentários divergem — comportamento idêntico; ação pendente do autor) |
| 3 (2026-07-12) | refino não re-rodava normalizadores nem invalidava aprovações/specs; sem diff para o autor | `worker/src/fundacao-refino.test.ts` (7 testes) | `refinarFundacao` só checava existência de docs e subia Storage | diff por hash antes/depois; `invalidarQualityCapitulos` (approved→stale); `estado/refino-impacto.json`; normalizadores unificados em `aplicarNormalizadoresFundacao` (criar/refinar/escrever); gate reavaliado pós-refino | typecheck ok; worker 348 testes verdes | invalidação é de Quality State (o texto dos capítulos não é reescrito automaticamente — ação humana/runner necessária); refino interrompido re-roda por attempts (idempotente por marcadores) |
| 2 (2026-07-12) | `criar_fundacao` aprovava só com Biblia+Estrutura+total>0; VOZ-CONSISTENCIA era aviso eterno; escrita iniciava sem gate | `worker/src/fundacao-gate.test.ts` (19 testes) | check fraco em `jobs.ts:464-473`; `sinalConsistenciaVoz` só console.warn | novo `fundacao-gate.ts` (gate determinístico + Quality State hash-bound + auto-registro de voz idempotente); wiring em `criar_fundacao` (bloqueia; projeto não vira `fundacao`) e preflight de `escrever_livro` (0 caps ⇒ bloqueia; retomada ⇒ aviso alto, estado gravado) | typecheck ok; worker 341 testes verdes | rubrica anti-genérico é heurística (viradas/sinopses/antagonista = warnings); coerência de personagens v1 cobre só o protagonista; retomada de livro vivo com fundação reprovada segue permitida (decisão registrada — publicação continua protegida) |
| 1 (2026-07-12) | entrevista aceita `completo:true` sem obrigatórios; prompt anuncia "4" enumerando 5; sem validação de schema | `worker/src/entrevista.test.ts` (20 testes) | código em `jobs.ts:430` fazia update+criar_fundacao com briefing arbitrário | novo `worker/src/entrevista.ts` (contrato determinístico: cobertura, schema, coerência, perguntas fallback) + wiring em `jobs.ts` | typecheck ok; worker 322 testes verdes | agente pode responder mal às perguntas determinísticas (limitado por attempts); sinônimos de cobertura são heurísticos |

