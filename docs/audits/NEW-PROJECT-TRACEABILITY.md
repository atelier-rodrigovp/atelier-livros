# Rastreabilidade — Novo Projeto até publicação

Atualizada em 2026-07-12 (pós-correções). "Teste" cita o arquivo que fixa a pós-condição.

| Etapa | Entrada | Responsável | Artefato | Pós-condição | Estado | Persistência | Teste | Falha possível | Recuperação |
|---|---|---|---|---|---|---|---|---|---|
| criar projeto | ideia e título | Web (`NovoProjeto.tsx`) | linha `projects` (rascunho) + job `entrevistar` | projeto do owner + job agendado; enqueue falho é visível e retryável | comprovado (E2E browser) | Postgres (RLS owner) | E2E 2026-07-12 + build | insert sem job (F-01) | UI detecta projeto sem job e oferece "Tentar novamente"; retomada por URL `?projeto=` |
| entrevistar | briefing + qa | worker/Claude + validador | `entrevista-out.json` → `briefing._interview` | contrato validado: cobertura dos 5 obrigatórios (perguntado≠inferido), schema, tipos, coerência caps×págs×palavras, skill conhecida; senão perguntas válidas/deterministicas; inválido = erro de job (attempts) | **comprovado** | disco + Postgres | `entrevista.test.ts` (21) + E2E real (5 turnos, teto exato) | JSON inválido, completo falso, skill inexistente (F-02/F-03) | attempts/max_attempts; perguntas determinísticas; erro visível na UI nova |
| fundação | briefing validado | worker/arquiteto + gate | Bíblia, Mapa, Estrutura, Perfil, ESTADO, 4 agentes + `quality/fundacao.quality.json` | gate determinístico: presença+parse+coerência cruzada+craft+voz; Quality State hash-bound; reprovada ⇒ QualityBlockedError (não vira `fundacao`) | **comprovado (unit)**; execução real pendente de despausa | WORK_DIR + Storage | `fundacao-gate.test.ts` (19) | arquivo presente porém incoerente (F-05); aviso eterno de voz (F-06) | refinar_fundacao; auto-registro de voz idempotente |
| refino | fundação + instruções | worker/arquiteto | fundação revisada + `estado/refino-impacto.json` | diff por hash; normalizadores re-executados; aprovações de capítulo→stale; specs afetadas listadas; gate reavaliado | **comprovado (unit)** | WORK_DIR + Storage | `fundacao-refino.test.ts` (7) | aprovação antiga permanece válida (F-07) | invalidação automática + impacto explícito |
| escrever | fundação aprovada | worker/runner/agentes | capítulos + `quality/capitulo-NN.json` | preflight: skill+manifest+gate da fundação (0 caps ⇒ bloqueia); loop medir→corrigir→reler→medir; texto inalterado reprova; teto BLOQUEIA (SPEC/REVISAO/DESMANEIRISMO, rc=3) | **comprovado (código+testes+evidência viva no banco)** | WORK_DIR | `quality-loop.acceptance`, `escrever-livro.integration`, runner rc=3 vivo | avanço sem pós-condição (F-08) | blocked_quality pausa o job com blockers nomeados; retomada do disco |
| revisão global | capítulos aprovados | runner (REVIEW/DESMANEIRISMO) | mestre + reports + estados renovados | REVIEW exige artefato real; DESMANEIRISMO recontagem book-wide; aprovações renovadas por hash só após recontagem limpa | comprovado (código + jobs vivos) | WORK_DIR | runner + `review-incremental.test.ts` | nota substitui evidência | nota é auxiliar; blockers objetivos mandam |
| publicação | mestre/EPUB aprovados | worker + gate + RPC | manifest hash-bound + objetos versionados | `decidePublication` (hash por capítulo; sem estado = bloqueia); staging por content-hash idempotente; `promote_publication` transacional é a única porta; trigger DB proposto | **comprovado (testes+SQL)**; E2E real pendente | Storage + Postgres | `publication-gate/transaction.test.ts`, `state-machine.test.ts`, `reliability-sql.test.ts` (7) | upload parcial/status prematuro (F-11) | staging + retry idempotente; falha não promove |
| fila/retomada | jobs queued | worker + RPC claim | jobs running/locked | claim atômico com exclusão por projeto (advisory lock); órfão requeue por lock stale; breaker qualidade×infra; aging anti-starvation; dedupe por índice único | **comprovado** (anomalia real explicada: producao_pausada) | Postgres | `claim.test.ts`, `fila.test.ts` (10), `retry-policy.test.ts` | starvation/duplicata (F-10) | prioridadeEfetiva + índice parcial (DDL pendente) |
| observabilidade | jobs+heartbeat | Web | badges/status | bloqueio de qualidade vence status do projeto; worker offline não mostra "Escrevendo"; rótulos distinguem concluído/pausado/interrompido | **comprovado (browser + testes)** | — | `status.test.ts` (+2) + browser 2026-07-12 | execução fantasma (F-12) | Realtime + polling fallback |
