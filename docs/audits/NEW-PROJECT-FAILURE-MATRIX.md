# Matriz de falhas — Novo Projeto

| ID | Etapa | Falha a reproduzir | Impacto potencial | Ambiente seguro | Estado |
|---|---|---|---|---|---|
| F-01 | criação | projeto criado e enqueue falha | rascunho órfão | adapter falso | **corrigido (UI)** — `NovoProjeto.tsx`: enqueue falho vira erro visível com "Tentar novamente" (`agendarEntrevista`); `sincronizar` detecta projeto sem job de entrevista e oferece retry; rascunho órfão passou a ser recuperável. Validar no browser (FASE 12) |
| F-02 | entrevista | `completo:true` sem obrigatórios | fundação genérica | fixture | **corrigido** — `entrevista.ts` recusa conclusão sem cobertura (perguntado ≠ inferido) e emite perguntas determinísticas; briefing vazio/incoerente reprova. Testes: `entrevista.test.ts` (20) |
| F-03 | entrevista | JSON inválido/tipos errados | job em loop ou dados corrompidos | fixture | **corrigido** — `validarSaidaEntrevista` valida schema/tipos/coerência/skill; `invalido` vira erro de job (attempts/max_attempts limitam). Prompt derivado de `CAMPOS_OBRIGATORIOS` (fim do "4 vs 5") |
| F-04 | entrevista | refresh/Realtime ausente | fluxo irrecuperável na UI | teste componente/browser | **corrigido (UI)** — retomada por URL (`/novo-projeto?projeto=<id>`); polling de fallback (12s) além do Realtime; job `error/paused` exibido com retry (fim do spinner eterno); duplo-clique guardado (`enviando`); `Projeto.tsx` oferece "Retomar entrevista" e não deixa gerar fundação com entrevista incompleta. Validar no browser (FASE 12) |
| F-05 | fundação | Mapa/Perfil/agente ausente | escrita começa incompleta | diretório temporário | **corrigido** — `fundacao-gate.ts`: presença+parseabilidade+coerência cruzada (capítulos Estrutura×ESTADO, skill, protagonista)+craft nos agentes; Quality State em `quality/fundacao.quality.json` vinculado a hashes; `criar_fundacao` e início de `escrever_livro` bloqueiam via QualityBlockedError. Testes: `fundacao-gate.test.ts` (19) |
| F-06 | fundação | craft injetada e marcador de voz ausente | aviso eterno sem pós-condição | diretório temporário | **corrigido** — `consistenciaVozAutomatica`: craft comprovada no perfil ⇒ registro automático auditável na Bíblia (hash+timestamp); divergência autoral preservada; craft não comprovada ⇒ blocker `VOZ_NAO_COMPROVADA`/`CRAFT_AUSENTE`. Idempotência testada |
| F-07 | refino | documentos mudam sem invalidar specs/hashes | capítulos incoerentes aprovados | fixture | **corrigido** — `refinar_fundacao`: snapshot de hashes antes/depois; re-executa os mesmos normalizadores da criação (`aplicarNormalizadoresFundacao`); aprovações de capítulo viram `stale` (`FUNDACAO_ALTERADA_POS_REFINO`); specs afetadas listadas; impacto em `estado/refino-impacto.json` + progresso do job; gate reavaliado e bloqueia refino que quebra a fundação. Testes: `fundacao-refino.test.ts` (7) |
| F-08 | escrita | texto inalterado após correção | falsa aprovação | teste existente; ampliar | **comprovado** — runner: micro-loop exige `exc_depois < exc_antes` (texto inalterado reprova); teto BLOQUEIA em SPEC_CAPITULO, REVISAO_CAPITULO e DESMANEIRISMO (`blocked_quality`, rc=3) — evidência viva: 2 jobs `paused/blocked_quality` no banco (53abdade, cae6a074). Comentários obsoletos ("2ª passada aceita") corrigidos no runner versionado. Publicação valida hash por capítulo (`stateForCurrentText`) |
| F-09 | escrita | blocker histórico fora do Quality State | publicação/avanço inconsistente | cópia isolada | **corrigido** — capítulo sem Quality State já bloqueia publicação (`QUALITY_STATE_MISSING`); backfill honesto criado: `backfill-quality.ts` + `scripts/backfill-quality-caps.ts` re-mede muleta/cadência e grava `pending`/`rewrite_required` (NUNCA `approved`; decisões do runner preservadas). Testes: `backfill-quality.test.ts` (4). `sem_revisao_por_capitulo`: confirmação explícita na UI (dialog + timestamp) e rótulo `reducao_qualidade` no progresso do job |
| F-10 | fila | dois jobs/trabalhadores no mesmo projeto | corrupção local | adapters/Postgres efêmero | **comprovado + 2 correções** — claim atômico via RPC `claim_job` (advisory lock + exclusão por projeto; fallback local proibido e testado); órfãos requeued por `locked_at` stale + keepalive 60s; breaker/backoff com distinção qualidade×infra (testados). Anomalia dos jobs presos 14d EXPLICADA: `producao_pausada=true` nos 2 projetos (intencional, verificado no banco — não é bug). Corrigidos: starvation por prioridade sem aging (`prioridadeEfetiva` +1/24h, `fila.test.ts`) e corrida de enqueue duplicado (índice único parcial `jobs_one_queued_per_project_tipo_uidx` em reliability.sql — DDL pendente de aplicação). Residual: `recuperarOrfaos`/`processamentoAtivo` sem teste automatizado (P3) |
| F-11 | publicação | upload ou DB falha | edição parcialmente pronta | adapters | **revalidado (código+testes+SQL)** — staging versionado por hash de conteúdo (`publications/<manifest-id>/…`, repetição idempotente); `promote_publication` (RPC plpgsql) é a única fronteira que torna `pronto` visível e roda por último; falha de upload/DB não promove; `advanceEditionStatus` proíbe `pronto` fora do gate (testado); frontend não escreve `editions.status`; `publishing_packages.status='pronto'` é tabela de pacote KDP, não promove edição. Residuais: sem trigger DB contra update direto de `editions.status` (SQL proposto, DDL pendente de autorização); EPUB↔mestre não comparado por hash no gate (EPUB_STALE é heurístico) |
| F-12 | observabilidade | status sem heartbeat | execução fantasma | teste/browser | **corrigido** — reproduzido no browser (site publicado): Dashboard mostrava "Escrevendo" para livro `paused/blocked_quality` e aba Escrita rotulava 1/34 como "concluído". Corrigidos: `displayProjectStatus` ganhou `qualityBlocked` ("Bloqueado por qualidade" vence status do projeto; job realmente ativo não é mascarado — `status.test.ts`); rótulo da aba Escrita agora distingue concluído/pausado—decisão pendente/interrompido/não iniciado. Página do projeto já mostrava "Pausado · motivo" (comprovado no browser). Residual: job `queued` de projeto com `producao_pausada` ainda exibe "Na fila" sem citar a pausa (P3) |

